{
  "name": "flowral",
  "version": "0.3.0",
  "description": "Flux + Rx.js",
  "author": "yuka2py",
  "license": "MIT",
  "main": "lib/flowral.js",
  "files": [
    "lib"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps-inline",
    "watch": "babel src --out-dir lib --watch --source-maps-inline",
    "test": "npm run build && mocha test/*.js"
  },
  "devDependencies": {
    "babel": "^5.8.34",
    "espower-babel": "^3.3.0",
    "power-assert": "^1.0.1"
  }
}
